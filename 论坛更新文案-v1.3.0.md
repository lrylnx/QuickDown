# 标题备选

1. 【更新】速下 QuickDown v1.3.0：下载完成角标提醒 + 右键打开文件，修复极小文件偶发接管失败的遗留 bug
2. 【原创】速下 v1.3.0 更新：接管弹窗尺寸修复、列表颜色残留修复、扩展小文件接管加固，新增完成角标弹窗

---

# 正文

## 前言

速下 QuickDown 更新到 **v1.3.0**（浏览器扩展同步升级 **v1.2.0**）。这个版本集中修了一波实测中反馈的问题——重点是那个**极小文件偶尔不被接管**的遗留老 bug——顺便加了下载完成右下角角标弹窗和右键一键打开文件。老用户覆盖安装即可，扩展自动更新。

## ✨ 新功能

**🔔 下载完成角标弹窗**
- 文件下载完成后，屏幕右下角滑入提醒：显示文件名，附带「打开文件」「打开文件夹」两个按钮，不用再去列表里翻
- 弹窗不抢焦点：看视频、写文档时不会打断当前操作，几秒后自动淡出
- 跟随设置里的「下载完成通知」开关，原系统通知保留

**📂 右键菜单新增「打开文件」**
- 下载列表右键直接打开文件，和「打开文件夹」并列，双击不再是唯一打开方式

**🌗 外观手动切换（自动 / 浅色 / 深色）**
- 设置 → 常规 → 外观：默认「自动（跟随系统）」，也可手动锁定浅色或深色
- 系统在深浅色之间切换时速下保持你选的外观，选择即时生效、随用随改

## 🐛 Bug 修复

**1. 浏览器接管极小文件偶发失败（遗留老 bug）**
- 现象：几十 KB～几百 KB 的小文件偶尔没被速下接管，浏览器自己把文件下完了；更烦的是，之前漏接管的任务会在下一次启动浏览器时突然弹出确认框
- 原因：扩展靠事件接管下载，小文件几十毫秒就下载完了，扩展响应的瞬间竞态已经输了；而且兜底清理逻辑先抹掉了浏览器下载记录，导致后面"删除残留文件"这步再也不执行，文件就留在磁盘上
- 修复（扩展 v1.2.0）：
  - 兜底清理改为先删磁盘文件、再抹下载记录，任何情况下不留残留
  - 扩展后台每 30 秒补扫一次浏览器里漏网的进行中下载（按任务去重，不会重复接管）
  - 实话实说：浏览器下载接口对毫秒级完成的小文件做不到绝对 100%，但现在即使竞态输了也不会在浏览器和磁盘留下任何痕迹，体验上就是"干净"的

**2. 接管确认弹窗尺寸忽大忽小**
- 现象：弹窗有时大一点有时小一点，浏览器刚打开的第一个任务尤其明显
- 原因：窗口尺寸被锁在"第一帧布局"的高度，而提示文字的显隐和换行会带动这个尺寸漂移
- 修复：尺寸固定 500×340，每次弹出完全一致

**3. 左侧任务列表颜色残留**
- 现象：鼠标划过列表后，多行背景色卡住不消失，出现好几种颜色叠在一起，深浅色模式都有
- 原因：悬停高亮用的系统级鼠标跟踪，在窗口失焦和列表滚动时收不到"鼠标离开"事件，颜色就冻在那了
- 修复：重写悬停逻辑，现在行背景只有三种确定状态——透明 → 悬停浅灰 → 选中品牌色，鼠标拿开立刻恢复

**4. 右键菜单灰色无用项**
- 已完成的任务不再显示灰色的「暂停 / 继续 / 重试」（下载都完成了还暂停啥）
- 菜单按状态分组：未完成任务状态操作在前；已完成任务直接就是「打开文件 → 打开文件夹 → 重命名…」，最常用的都在最上面

## 🎨 界面优化（一并发布）

- 全新应用图标，深浅色模式自适应
- 菜单栏黑白菜单图标加大，更清晰
- 浅色模式窗口背景发灰的问题修复
- 分割线、次级按钮统一设计语言，深色模式下清晰可见
- 接管确认弹窗保证置顶弹出，不会被压在其他窗口后面
- 全部按钮加入悬停反馈：主按钮悬停提亮 + 光晕上浮，次按钮底色加深 + 描边变亮，配手型光标，按下回缩，弹窗出场淡入上浮
- 列表悬停/选中颜色平滑过渡，详情面板切换淡入，下载中状态点呼吸闪烁

## ⬇️ 更新方式

1. 下载新的一键安装 pkg，双击安装（自动装到「应用程序」并更新浏览器扩展，无需手动操作）
2. 如果浏览器扩展图标显示未连接，重启一次浏览器即可
3. 通用版（Apple 芯片 + Intel），约 2.5MB

下载链接：（这里放你的链接）

---

有任何 bug 或建议欢迎回帖，会持续维护。
