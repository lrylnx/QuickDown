# 标题备选

1. 【分享】自研 macOS 原生下载管理器「速下」：8线程加速 + 网页视频嗅探 + m3u8 一键合并 MP4，全中文免费
2. 【原创】写了个 2MB 的 macOS 下载器，把 NeatDownloadManager 换掉了：浏览器接管、视频嗅探、中文分类都有
3. 【分享】速下 QuickDown：原生 SwiftUI 下载管理器，IDM 该有的都有，还支持 m3u8 分片自动合成 MP4

---

# 正文

## 前言

之前一直用 NeatDownloadManager（NDM），可惜界面是英文的、更新也停在 2021 年。想汉化它却发现字符串全写在编译后的二进制里，根本汉不干净。于是干脆用 SwiftUI 从头写了一个全中文的原生下载管理器——**速下 QuickDown**，2MB 左右的安装包，功能对标 IDM/NDM，还多了网页视频嗅探和 m3u8 合并能力。

## 功能一览

**⚡ 下载核心**
- 多线程分段加速：默认 8 段并行（可调 1~32），自动探测服务器是否支持 HTTP Range，不支持时自动回退单连接（协议限制，IDM 也一样）
- 断点续传：暂停、退出后继续下载不丢进度
- 并发队列：同时下载数可调，其余自动排队
- 实时网速、进度、剩余时间显示
- 全局限速、代理（含认证）、开机自启、完成系统通知

**🖥️ 浏览器接管（Chrome / Thorium / Edge 等 Chromium 内核）**
- 自带中文扩展：浏览器下载自动转交速下，右键菜单「用速下下载」
- 可选"每次询问"模式、按文件类型过滤、排除指定站点

**🎬 网页视频嗅探**
- 打开含视频的网页，右下角自动弹出绿色「🎬 N」按钮，点开就是页面里所有视频资源（m3u8 / ts / mp4 / flv…），一键下载
- 支持 iframe 内嵌播放器（webRequest 全量捕获）
- **m3u8 分片流自动下载全部 ts 分片（支持 AES-128 解密）→ 合并 → 无损转封装为完整可播放的 MP4**，一个碎片都不留
- 实测：Apple 官方测试流 100 个分片，下载+合并+转封装后 600 秒完整可播放

**📁 下载自动分类**
- 按扩展名自动归入 6 个中文文件夹：**视频 / 音乐 / 压缩包 / 图片 / 应用程序 / 其他**，列表和详情里都有彩色分类标签

**✨ 其他**
- 全中文原生界面，菜单栏快捷图标
- 轻量：通用版（Apple 芯片 + Intel）约 3.8MB
- 数据本地保存（JSON 原子写入），无任何联网上报

## 实测数据（本机）

| 场景 | 结果 |
|---|---|
| 10MB 文件 4 段并行 | 0.34 秒，MD5 与源文件完全一致 |
| 100 分片 HLS（10 分钟） | 全片下载 + 合并 + 转封装 MP4，可正常播放 |
| AES-128 加密 HLS | 解密正确，输出与明文版大小一致 |
| 断点续传 | 中途暂停 → 继续 → 完整文件 |

## 安装

1. 下载解压（通用版支持 Apple 芯片和 Intel）
2. 终端执行 `./install.sh`（自动装到 /Applications 并注册扩展），或手动拖入应用程序
3. 首次打开若提示"无法验证开发者"：右键 → 打开（本地 ad-hoc 签名，正常现象）
4. 浏览器 `chrome://extensions` → 开启开发者模式 → 加载「浏览器扩展」文件夹

## 注意事项（先说清楚，避免误会）

- **下载速度取决于服务器**：支持 Range 的网站才能多线程；不支持的就只能单连接（这不是软件问题，IDM/NDM 同样如此）
- **浏览器里看到 1 字节的残留文件是正常的**：那是扩展取消浏览器下载留下的痕迹，真正的完整文件在速下里下载（下载目录 + 完成通知）
- 若之前装过 NDM：请移除它的扩展并退出 NDM（两者共用 10007 端口）
- 暂不支持：HLS 直播流追播、SAMPLE-AES 部分加密、Safari 扩展（需开发者证书）

## 下载

（这里放你的分享链接）

---

**补充说明（备用）**：作者后续计划加入 HLS 直播下载、定时限速等。有任何 bug 或建议欢迎回帖，会持续维护。
